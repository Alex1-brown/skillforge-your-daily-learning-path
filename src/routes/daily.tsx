import { createFileRoute } from "@tanstack/react-router";
import { Check, Clock3, Play, RotateCcw } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { Button } from "@/components/ui/button";
import { dailySteps } from "@/lib/demo-data";
import { useDemoProgress } from "@/hooks/use-demo-progress";
export const Route=createFileRoute('/daily')({head:()=>({meta:[{title:'Daily Task — SkillForge'},{name:'description',content:'Complete today’s focused learning session.'},{property:'og:title',content:'Daily Task — SkillForge'},{property:'og:description',content:'Complete today’s focused learning session.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Daily});
function Daily(){const {completed,toggle}=useDemoProgress();const done=completed.length===3;return <AppShell title="Today's focus" eyebrow="Day 24 · Python foundations" action={<div className="flex items-center gap-2 text-sm font-bold"><Clock3 className="size-4 text-primary"/>17 min</div>}><div className="max-w-3xl"><div className="rounded-md border border-border bg-card p-5 sm:p-8"><div className="flex items-start justify-between gap-5"><div><p className="text-xs font-bold uppercase text-primary">Daily mission</p><h2 className="mt-2 text-2xl font-bold sm:text-3xl">Make code reusable</h2><p className="mt-3 max-w-xl text-sm leading-6 text-muted-foreground">Learn the building block that turns repeated steps into clean, reusable logic.</p></div><div className="hidden size-14 place-items-center rounded-full border-4 border-primary text-sm font-bold sm:grid">{Math.round(completed.length/3*100)}%</div></div></div><div className="mt-4 space-y-3">{dailySteps.map((step,i)=>{const checked=completed.includes(step.id);return <article key={step.id} className={`rounded-md border p-5 transition-colors ${checked?'border-primary/40 bg-accent/40':'border-border bg-card'}`}><div className="grid grid-cols-[auto_minmax(0,1fr)] gap-4"><button aria-label={`${checked?'Undo':'Complete'} ${step.title}`} onClick={()=>toggle(step.id)} className={`grid size-9 shrink-0 place-items-center rounded-md transition-colors ${checked?'bg-primary text-primary-foreground':'border border-border bg-secondary text-muted-foreground hover:border-primary'}`}>{checked?<Check className="size-4"/>:<Play className="size-4"/>}</button><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className="text-[10px] font-bold text-primary">0{i+1} · {step.type}</span><span className="text-xs text-muted-foreground">{step.minutes} minutes</span></div><h3 className={`mt-1 text-lg font-bold ${checked?'line-through opacity-70':''}`}>{step.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{step.text}</p></div></div></article>})}</div>{done&&<div className="mt-4 rounded-md border border-primary/40 bg-accent p-5 text-center"><p className="font-display text-xl font-bold text-accent-foreground">Session complete. Streak secured.</p><p className="mt-1 text-sm text-muted-foreground">You invested 17 focused minutes today.</p><Button variant="ghost" className="mt-3" onClick={()=>dailySteps.forEach(s=>completed.includes(s.id)&&toggle(s.id))}><RotateCcw/>Reset demo</Button></div>}</div></AppShell>}